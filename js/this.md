### 关于 this

this 提供了一种更优雅的方式来隐式“传递”一个对象引用
this 是在函数被调用时发生的绑定，它指向什么完全取决于函数在哪里被调用。

#### 调用位置

#### 绑定规则

##### 默认绑定

独立函数调用

```js
function foo() {
  console.log(this.a);
}
var a = 2;
foo();
```

##### 隐式绑定

##### 显示绑定

##### new 绑定
